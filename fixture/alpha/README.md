# alpha

A forge-conformance fixture repository with a deliberately wordy
readme so that line numbers are interesting and stable.

Nothing on this line matters. The fixture exists to be served by an
adapter under test; its bytes are frozen and manifest-hashed.
